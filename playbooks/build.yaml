- name: Build Docker Image
  hosts: dockerservers
  gather_facts: false
  remote_user: root
  tasks:
    - name: Building Docker Image
      docker_image:
        name: team14:latest  # Docker image name
        source: build
        build:
          path: ~/team14%402  # Path with @ replaced by %40
        state: present
        force_source: true
