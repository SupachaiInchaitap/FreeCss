- name: Build Docker Image
  hosts: dockerservers
  gather_facts: false
  remote_user: root
  tasks:
    - name: Building Docker Image
      docker_image:
         name: omegalul:latest
         source: build
         build:
            path: /var/lib/jenkins/workspace/omegalul  # Adjust this path
         state: present
         force_source: true
